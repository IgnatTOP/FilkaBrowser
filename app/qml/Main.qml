import Filka

// Filka Browser — application root. The entry point is simply the first browser
// window; every window is a self-contained BrowserWindow (own tabs/workspaces,
// shared profile), and Ctrl+N spawns additional ones.
BrowserWindow {}
